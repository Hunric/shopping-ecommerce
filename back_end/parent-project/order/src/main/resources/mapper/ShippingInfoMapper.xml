<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hunric.mapper.ShippingInfoMapper">

    <sql id="Base_Column_List">
        shipping_id, user_id, receiver_name, receiver_phone, province, city, district,
        detail_address, is_default
    </sql>
    
    <!-- 根据ID查询收货信息 -->
    <select id="findById" resultType="com.hunric.model.ShippingInfo">
        SELECT <include refid="Base_Column_List" />
        FROM shipping_info
        WHERE shipping_id = #{id}
    </select>
    
    <!-- 根据用户ID查询收货信息列表 -->
    <select id="findByUserId" resultType="com.hunric.model.ShippingInfo">
        SELECT <include refid="Base_Column_List" />
        FROM shipping_info
        WHERE user_id = #{userId}
    </select>
    
    <!-- 插入收货信息 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="shippingId" parameterType="com.hunric.model.ShippingInfo">
        INSERT INTO shipping_info (
            user_id, receiver_name, receiver_phone, province, city, district,
            detail_address, is_default
        ) VALUES (
            #{userId}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district},
            #{detailAddress}, #{isDefault}
        )
    </insert>
    
    <!-- 更新收货信息 -->
    <update id="update" parameterType="com.hunric.model.ShippingInfo">
        UPDATE shipping_info
        SET receiver_name = #{receiverName},
            receiver_phone = #{receiverPhone},
            province = #{province},
            city = #{city},
            district = #{district},
            detail_address = #{detailAddress},
            is_default = #{isDefault}
        WHERE shipping_id = #{shippingId}
    </update>
    
    <!-- 根据ID删除收货信息 -->
    <delete id="deleteById">
        DELETE FROM shipping_info
        WHERE shipping_id = #{id}
    </delete>
    
    <!-- 重置用户的默认地址 -->
    <update id="resetDefaultByUserId">
        UPDATE shipping_info
        SET is_default = false
        WHERE user_id = #{userId}
    </update>
    
</mapper>